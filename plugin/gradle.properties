# Local machine config: register the JDKs used to build this module.
# The `plugin` module requests a Java 17 toolchain (jvmToolchain(17)), and as an
# included build it does not inherit the root project's gradle.properties.
# These paths are machine-specific — adjust or remove before committing upstream.
org.gradle.java.installations.paths=/home/fenn/Repo/light-phone/tools/jdk-21.0.12+8,/home/fenn/Repo/light-phone/tools/jdk-17.0.20+8
